{
  "name": "demo",
  "source": "src/index.html",
  "homepage": "https://juicy-captain.github.io/ems-double",
  "scripts": {
    "start": "parcel",
    "deploy": "rm -rf dist/ && npm run start && gh-pages -d dist"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "gh-pages": "^6.1.1",
    "parcel": "^2.12.0"
  }
}
